pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "BitFitAppPart1"

// List your modules here if you have them
include(":app") // Assuming your main module is named 'app'
